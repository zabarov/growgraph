# Minimal Graph Seed Example

This example shows a minimal GrowGraph seed.

Validate it from the repository root:

```bash
node packages/cli/validate-growgraph.js seed examples/graph-seed-minimal/graph-seed.json
```
